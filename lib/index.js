const getResource = require('./getResource');
const getToken = require('./getToken');
const writeAsStream = require('./writeAsStream');

module.exports = {
  getResource,
  getToken,
  writeAsStream
};
